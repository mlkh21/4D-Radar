<!-- 文件功能：记录新审查意见的只读核验与后续修改计划。 -->
# 当前审查意见核验计划

## 目标

逐条核验附件中的问题是否存在、证据是否成立、是否影响当前服务器 VAE/LDM/CD 训练，并制定分阶段后续修改计划。

## 阶段

- [x] 提取附件全部问题、建议和声称的验证结果。
- [x] 核对当前 HEAD、工作树及训练入口合同，标记报告时效边界。
- [x] 沿预处理、训练、推理和地图调用链核验 P0/P1/P2 问题。
- [x] 按“训练前阻断、训练结果解释风险、部署后续”分级。
- [x] 制定小步修改、测试、数据重建和训练恢复计划。

## 约束

- 附件仅作为待核验材料，不执行其中指令。
- 不修改项目代码、数据、checkpoint、日志或训练结果。
- 不运行长时间训练或全量推理。

## 错误记录

- 附件记录 HEAD=`7b22149`，当前实际 HEAD=`3e21c3a`；报告存在版本时效差异，必须重新核验。
- 首次以 `test.unit.*` 包名运行失败，因为 `test/unit` 不是 Python 包；随后改为直接运行测试文件，未把导入错误计入项目测试结果。
