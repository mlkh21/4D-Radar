<!-- 文件功能：记录外部审查意见核验过程和只读验证进展。 -->
# 审查进展

## 2026-08-21

- 已读取项目规则与 planning-with-files 技能，确认本轮只分析和制定计划。
- 已读取附件前半部分；因输出截断，正在按行号读取完整 488 行。
- 已建立独立 planning 目录，未修改项目源码或运行训练。
- 已完整读取附件 488 行，提取 7 项 P0、10 项 P1 和 P2/P3 摘要；阶段 1 完成。
- 进入当前工作树证据核验，先核验高风险 P0，再检查 P1/P2 是否已被现有未提交修改覆盖。
- 初步核验发现附件 Git 快照已过期；P0-01/02/03 的核心事实仍存在，但具体修复边界尚需读完矩阵方向、target mask 和地图风险调用链。
- 完成全部 P0/P1/P2/P3 当前代码核验：P0-01 至 P0-06 均存在，P0-07 为训练状态；P1 中 9 项存在、PointCloud schema 为部分修复。
- 数据反例确认 000000 在原始 80--120 m 有 48 个 LiDAR occupied，却被 target 清零；训练尺寸下仍有 1936 个远距 observed 体素作为负类进入 VAE。
- 标定闭环复算确认最大旋转元素差 0.0508489、最大平移分量差 0.151702 m；当前 garden 解包 IR 为 8-bit 三通道而非 16-bit，纠正了附件的位深断言。
- 发现附件遗漏的正式训练隐形依赖：`.tmp_train_dataset` 使 CalibrationProvider 找不到 `Data/config`，实际样本得到 mock calibration/default intrinsics；若直接开始 LDM 会污染正式结果。
- 形成 8 个实施阶段的详细计划，包含修改文件、RED/GREEN 验收、80/120 m 决策门、监督/体素/checkpoint/指标影响和二次确认点。
- 本轮没有修改任何项目源码、数据、checkpoint、训练日志或实验结果，也没有启动训练；仅更新 `.planning` 审查记录。
- 用户已确认实施计划；本轮进入实施 0--2 和实施 3 只读审计，不执行全量数据重建或长时间训练。
- 重新核验当前 HEAD 为 `02c4fd3`，除 `.planning` 记录外源码工作树干净。
- 已读完 `test/AGENTS.md` 并复核 checkpoint 主调用链；实施 0 将采用结构化 data protocol、按阶段验证和显式 legacy load 三项最小接口。
- 实施 0 首个代码补丁已落地：正式 checkpoint 协议提升到 v2，新增结构化 data protocol 校验、`target_stage` 父链验证和显式 legacy 诊断开关；尚未运行测试。
- 第一次 checkpoint 单测运行在 construct case 导入完整推理模型时触发 OpenMPI sandbox socket 限制并中止；此前 CLI case 已通过。该错误已记录，将把纯协议测试与环境相关 construct 测试分开运行。
- 首次分离测试错误地假定 `test.unit` 是 package，收到 `ModuleNotFoundError`；下一次改用文件模块加载。
- checkpoint 纯协议测试（排除环境相关 construct case）12 项通过；开始把相同身份门禁接入 VAE/LDM/CD payload 和 resume 路径。
- 已把 data protocol 写入 VAE/LDM/CD payload，并在父权重/续训权重构建模型或恢复优化器前校验；train/CD/inference 的安全加载开始统一为 weights-only 默认。
- 实施 1/2 的 Dataset 核心补丁已落地：支持显式 `scene_names/calibration_dir`、正式模式禁 mock、LiDAR-frame 选择 `calib_livox_to_thermal.txt`、删除加载时固定 0.01m 位移，并校验外参有限性/正交性/determinant；尚待测试与 launcher 接线。
- 沙箱外完成首轮 Dataset/IR 回归：thermal 5/5、Dataset 15/15 通过；inference 27/31 通过，4 项是新增属性和 legacy 测试夹具接口问题，不是几何逻辑失败，已记录待修。
- 修复 inference 测试夹具兼容后，多模态推理接口 31/31 通过。
- manifest schema v2 已实现 training/deployment profile：training 精确绑定四模态和监督 provenance，deployment 只要求 Radar/IR 但仍绑定同步与三组标定 provenance；legacy v1 仅在未指定正式 profile 时可验证。
- 预处理已改为读取 Radar--LiDAR signed delta，并按 `p_ref=p_sensor+v*(t_sensor-t_ref)` 只移动非参考点云；Radar--IR 同步记录新增 signed delta；`velocity_mode=none` 的 Doppler metadata 改为 raw。
- Dataset/预处理均输出三外参直接链与组合链残差审计；正式 Dataset 缺 thermal 内参时 fail-closed。
- 正式训练入口不再创建 `.tmp_train_dataset` 或场景软链接；配置新增显式场景、标定目录、真实 IR/标定门禁及 formal v2 data protocol artifact。正式部署入口改用 deployment manifest 和显式 calibration_dir。
- 语法、shell 解析和 `git diff --check` 首轮通过。Conda manifest 单测首次运行暴露 Python 3.8 不会延迟求值 `str | None` 注解；已加入 future annotations，待复测。

## 2026-08-21 第一批实施完成

- 实施 0--2 已完成：checkpoint/data protocol v2、training/deployment manifest profile、显式场景/标定、真实 IR fail-closed、LiDAR→Thermal 坐标链和 signed timestamp delta 均已接入。
- 正式训练不再使用 `.tmp_train_dataset` 场景软链接；训练 preflight 交叉检查 manifest、target policy、Radar--IR sync 和 calibration provenance。
- 正式推理新增 VAE 与 LDM/CD `data_protocol` 一致性检查，并把 deployment scene、Radar 输入目录、manifest、当前 calibration 与 checkpoint 标定 hash 交叉绑定；身份写入 `inference_run.json`。
- garden 4013 帧远距审计 v2 完成：80--120 m raw 点 372780、LiDAR occupied 365069、target occupied 0；3934 帧存在远距 LiDAR occupied，当前 target 保留率为 0。
- 80--120 m raw 点仅为 0--80 m 的 0.404%，occupied voxel 为 1.488%；抽样 ray coverage 分别约 0.194% 与 3.845%。128 帧 Radar--LiDAR 远距 1 m/2 m 匹配率均值为 5.23%/23.24%。
- 标定闭环仍有 rotation max abs 0.05085、translation L2 0.20383 m；IR 抽样证明原始是 uint8 三通道且通道有差异，现预处理结果为三通道完全相同的 grayscale。
- 审计建议 formal v2 先采用 0--80 m 模型/评价范围，80--120 m 保持 unknown；不建议把当前 0 标签直接扩成 120 m free/supervised。
- 轻量回归通过：checkpoint 13、VAE 24、Dataset 15、manifest 12、thermal 6、motion 9、inference 33、formal inference 11、normalization 12、far audit 4，另两份 CD 接口测试通过；Python/Shell 编译与 `git diff --check` 通过。
- 完整 `test_airborne_multimodal_refactor.py` 中既有完整 3D CPU forward 两次超过 90 秒无输出后终止，仅相关几何 5 项通过；该整文件未计为通过。没有运行预处理重建、mini/full training、GPU 推理或正式评价。
- 首版 audit v1 的 raw 统计只限制 x，已保留不覆盖；v2 同时限制 XYZ 正式体素盒，后续只采用 v2 报告。
- 已知下一阶段依赖：持久 observed mask、唯一 split artifact、train-only normalization 和 formal data protocol artifact 尚未实现；正式 launcher 继续因 `range_pending` 和 artifact 缺失而 fail-closed。
- 错误记录：一次使用 `python -m unittest` 加多个 `test/unit/*.py` 因目录不是 package 而全部未执行，随后逐文件直接运行并全部复核通过；远距审计第一次传入旧 Raw 根缺 sync CSV，输出目录创建前退出，改用 header-time candidate Raw 后成功。

## 2026-08-21 0--80 m 分支实施启动

- 用户已确认推荐分支：formal v2 模型/评价范围 0--80 m，80--120 m 在地图端保持 unknown，模型张量保持 `32×128×128`。
- 本阶段授权边界：实现持久 observed mask、唯一 temporal split、train-only normalization/data protocol，并运行 2--4 帧 fresh smoke；不执行全量数据重建、mini/full training 或正式推理。
- 已按 planning-with-files 要求完成 session catchup；继续保留当前工作树中第一批未提交修改，不覆盖旧 v1 数据、artifact、checkpoint、日志或结果。
- 已复核预处理落盘、Dataset 加载、VAE loss、时间切分、normalization 与 manifest/data protocol 调用链；确认可以通过小步补齐数据契约，无需大范围修改模型结构。
- observed mask RED 首次因测试未加入项目根路径而失败，修正测试入口后按预期因新模块缺失失败；没有误把环境错误计作 RED。
- 新增共享 `observed_mask.py`，预处理逐帧落盘带 protocol/shape/pc_range 的稀疏 mask；Dataset 新增正式模式门禁并保留 legacy 运行时重建路径。
- observed mask 聚焦单测 4/4 通过；尚未运行现有 Dataset 回归或 fresh 预处理 smoke。
- training manifest v2 已扩展为五模态，正式要求逐帧 `observed_mask`；legacy v1 仍只扫描原四模态，避免历史诊断数据被无关破坏。
- 新增内容寻址的 `temporal_split_v1` 与 CLI：按 Radar 时间形成连续 train/purge/validation，正式模式拒绝零 purge、缺验证集和数据身份漂移；聚焦单测 3/3 通过。
- Dataset 支持按 split 的 scene/frame_id 精确收集；unified formal v2 不再运行时按下标重算切分，并交叉校验 split 文件 SHA 与 data protocol。
- normalization builder 已改为正式 artifact 必须绑定 split 且只扫描 train frame；无 split 或截帧输出自动为 formal=false。生成器 4/4、normalization 协议 12/12 通过。
- 新增 formal data protocol 生产/加载模块与 CLI；它从 manifest、split、target policy、observed records、Radar--IR sync 和 IR 标定自动派生同一 `formal_data_v2`，临时全链测试 4/4 通过。
- 对现有 garden 4013 帧完成只读时间相关性审计：约 0.1 s median Jaccard=0.289；2 s median=0.047 但 p90=0.183；3 s median=0.036、p90=0.075。因此正式默认采用 3.0 s purge，约排除 30 帧（约 0.75%）。
- default/preprocess/train/inference/evaluation 正式路径与网格已切换到新 `formal_v2_80m_86p8_v1`；旧 full120 数据、artifact 和结果路径均未删除或覆盖。
- 代码审查发现模型 80 m 与地图 120 m 不能复用同一个 `pc_range`：已新增 evidence/map 双范围接口，模型 128 格不再被拉伸到 120 m，80--120 m 地图单元保持 unknown。地图 RED/GREEN 反例通过，完整 mapping 回归 37/37 通过。
- 第一轮兼容回归通过：manifest 12/12、Dataset 15/15、normalization 12/12、formal inference 11/11、Shell/Python 静态解析通过。
- 4 帧 fresh 预处理 smoke 完成：garden training manifest 为五模态、frame_count=4、content hash=`a0a70ea5...be34`；正式 Dataset 返回 4 个 `4x32x128x128` 样本并确认 persisted observed/real IR/real calibration。
- 新增/修改协议的聚焦回归已通过：observed 4、temporal/data protocol 4、manifest 12、Dataset 15、normalization builder 4、normalization protocol 12、mapping 37、formal inference 11、mini launcher 11、VAE checkpoint 24、checkpoint chain 13，CD 两组入口测试通过。
- 没有运行全量预处理、mini/full training、GPU inference 或正式评价；旧数据、normalization、checkpoint、日志和结果均保留。
- 最终接口审查未发现正式 launcher 残留旧 full120/临时 dataset root；发现独立 CD 正式门禁弱于 unified，准备补齐并加入静态入口回归。
- 独立 CD 已补齐 scene/真实 IR/真实标定/LiDAR frame/持久 observed 五项 formal 门禁；CD 入口回归与 py_compile 通过。首次沙箱内运行被 OpenMPI socket 权限阻断，沙箱外原命令通过，未计作代码失败。
- 最终复跑 87 项聚焦单测与 CD 脚本测试全部通过；新增 Python 编译、6 个正式 shell `bash -n` 和 `git diff --check` 全部 exit 0。本轮授权范围完成，下一项为 deployment-profile 数据视图生产；全量 artifact/训练继续等待独立执行条件。
- 用户确认进入严格 deployment-profile 数据视图生成链；本子阶段只实现生产器、严格校验和 2--4 帧 smoke，不运行训练/GPU 推理。
- 错误记录：首次 session catchup 调用了不存在的 `session_catchup.py`；技能实际脚本为 `session-catchup.py`，下一次使用正确文件名，未影响项目文件。
- deployment view RED 已建立 4 项：生产/无监督模态、provenance 漂移、额外目录或内容篡改、输出不覆盖。首次运行全部因 `deployment_view` 模块不存在而失败；其中两项为异常正则未匹配模块缺失文本，属于同一预期 RED 根因。
- deployment v3 基础 GREEN 完成：新增自包含生产/校验模块和 CLI；临时两帧 hardlink/copy、父 manifest 绑定、preflight 漂移、额外真值目录、内容篡改和非覆盖 4/4 通过。
- 已把推理 Python 身份门禁改为完整 view validator，三个正式 launcher 改为根级 dataset receipt/场景集合一次性校验；`preprocess-v2.sh` 新增第 8 步生成 loop3 hardlink deployment view，尚待兼容测试更新与 smoke。
- 首轮协议回归中 manifest 12/12、deployment view 4/4 通过；formal inference 在首项导入评价模块时因沙箱 OpenMPI socket 权限退出，尚未执行断言，按既有处理在沙箱外复核，不计作代码失败。
- 沙箱外 formal inference 11/11、多模态推理接口 33/33 通过；正式 Python 入口确认会把父 training manifest、receipt、deployment manifest、当前标定与 checkpoint data protocol 交叉绑定。
- 4 帧 deployment smoke 第一次在输出创建前失败：传入的 header-time Raw 根缺少 `garden/radar_ir_sync.csv`。生产器未猜测或跳过 provenance；需按 training manifest 记录定位实际同步文件并调整显式接口。
- 实查确认同步文件位于 training scene；准备移除 deployment builder 的 Raw 根参数并把同步 CSV 复制进视图。第一次失败没有创建 deployment 输出目录。
- 已移除 Raw 根参数并复制 scene 内同步 CSV；manifest/deployment 回归再次 12/12、4/4 通过。真实 garden 4 帧 hardlink deployment smoke 创建和二次验证成功，输出位于 `test/result/comparison/formal_v2_80m_deployment_smoke_v1`。
- 最终静态检查首轮通过：新增 Python 编译、四份 shell `bash -n`、CLI `--help`、mini launcher 11/11 和 `git diff --check` 全部成功；正在修正 hardlink 在服务器传输后的审计字段语义。
- hardlink 字段已明确为 `materialization_mode_at_creation`；复制到服务器展开 inode 后仍通过内容协议测试。最终回归 82 项全部通过，Python 编译、四份 shell 语法和 `git diff --check` 成功；未运行全量数据、训练或 GPU 推理。
- 用户确认继续实施 deployment observed/frame/risk 安全契约；本轮先处理不需要重训的推理 mask、地图正式门禁、显式坐标链和三态风险，不修改 `UncertaintyHead` 或 checkpoint 结构。
- 已沿真实入口核对：推理缺 observed 输出，streaming mask/pose 为 optional+identity legacy，风险查询的高不确定性分支仍 fail-open；准备在现有 mapping/inference 测试中增加 RED 反例。
- 已确定兼容接口：formal inference 自动保存 Radar 射线 mask；formal map 要求 run metadata+mask+pose+LiDAR→body，核心组合 `T_local_body@T_body_voxel`；风险返回三态但保留 `is_risky`。
- 现有 formal inference launchers 均带 `--save_voxel`，Dataset 已有严格 R/T 解析与体素物理 crop/resize；实现将复用这些函数，并让 inference metadata 绑定逐帧 mask hash，不新增隐式目录猜测。
- RED 完成：推理 3 项分别因 metadata 参数、射线 mask helper、fresh output helper 缺失而 ERROR；地图 4 项分别因 `T_body_voxel`、安全距离 helper、三态键和 `--formal_mapping` 缺失而 ERROR。失败均命中预期旧接口，未创建数据结果。
- 错误记录：首个 inference 主流程整块补丁因 `RadarGenerator` 字段上下文不匹配而整体未应用；改为读取实际窄行段后分块应用，不重复同一大补丁。
- 错误记录：streaming 参数+validator 同文件组合补丁因函数上下文顺序匹配失败而整体未应用；后续拆为 parser 与 validator 两个小补丁。
- 首轮 GREEN：推理 3/3、地图 4/4 聚焦反例全部通过。formal inference 现发布 Radar endpoint ray observed 收据；地图核心保存 body/voxel/local 三段变换，风险查询 unknown fail-closed 并记录动态安全距离。
- 为保持 streaming `--help` 不加载 Torch/OpenMPI，把共享 R/T 刚体解析放入轻量 `geometry_protocol.py`；此前 Dataset 临时包装已撤回，没有改变 Dataset 行为。
- 完整聚焦回归通过：multimodal inference 35/35、probabilistic mapping 43/43，共 78 项；包括 formal 完整 fixture、mask 篡改前置拒绝、legacy pose/dynamic/DEM/observed 兼容。
- 静态审查进入第二轮：准备补 formal observed→deployment calibration 交叉绑定，并拒绝四类尚无统一 frame/provenance 的可选地图输入；不删除其 legacy 功能。
- 第二轮加固聚焦测试 4/4 通过：formal metadata、完整地图 fixture、标定身份错配和未绑定 uncertainty 拒绝均符合预期。
- 真实 garden 4 帧 observed smoke 通过：endpoint 数 983/964/952/994，ray observed 数 11744/11280/11222/11399；全部 endpoint 被 mask 覆盖，Radar→LiDAR 标定 SHA 为 `e50426da...be08d`。
- 错误记录：遮挡语义聚焦测试首次使用 `python -m unittest test.unit...`，因 `test/unit` 不是 package 而在收集阶段报 `ModuleNotFoundError`，未执行任何断言；后续改为直接运行测试文件。
- 补齐 Radar 射线遮挡语义：所有 endpoint 仍标记 observed，但每个离散方向只向最近 endpoint 铸造 free-space ray，不清空近端障碍后的间隔。推理接口 36/36 通过。
- 真实 garden 4 帧遮挡语义 smoke 复测通过：endpoint 数仍为 983/964/952/994，observed 数收缩为 11727/11263/11208/11384，所有 endpoint 仍在 mask 内；数量下降为防止远端遮挡空间被误标 free 的预期变化。
- 最终回归共 103 项通过：multimodal inference 36/36、probabilistic mapping 45/45、formal inference 11/11、mini launcher 11/11。
- 静态验收通过：6 份相关 Python 文件编译、streaming map `--help`、3 份正式 inference shell `bash -n` 与 `git diff --check` 均 exit 0。
- 本子阶段完成；未运行训练、模型前向、GPU 推理、全量数据生成或正式地图回放，也未删除/覆盖旧数据和实验产物。真实 formal map 运行等待外部 LiDAR→body 标定和 body→local pose；数据/权重相关的 Radar validity/UncertaintyHead 留待后续独立授权。
- 用户授权继续 LiDAR→body / body→local 获取链；本轮只生成 diagnostic candidate 和 audit，不修改 formal 标定目录、deployment receipt 或地图输出。
- 已确认候选数据边界：loop3 6432 Radar 帧、6445 GT pose，前 4 帧超出 GT 时间范围；正在以 RED 单测冻结外参组合、SLERP、无外推和 fresh 输出语义。
- RED 已验证：3 项合成测试在收集时因目标诊断脚本不存在报 `FileNotFoundError`，未进入断言或创建输出；下一步实现最小 GREEN。
- GREEN 完成：独立候选脚本、无外推/fresh 输出和正式 loader 内容门禁共 4 项合成测试通过。
- loop3 fresh 诊断结果已生成并自检：6162 covered、270 uncovered；两套 pose CSV 时间严格递增、四元数归一化、哈希一致且全部为 `formal=false`。没有训练、GPU 任务或正式地图回放。
- 最终轻量回归通过：候选协议 4/4、概率地图 45/45，4 个相关 Python 文件编译、CLI `--help` 与差异格式检查成功。
- 用户授权按“权威定义优先、实物/CAD交叉验证、点云一致性只作反证”继续；已建立只读来源审计→四假设冻结→RED/GREEN→loop3 小规模比较→复审记录的执行阶段。
- 已找到 loop3 原始三段 ROS bag 和原始下载副本；下一步在 source ROS Noetic 环境后读取 bag topic/TF，裸 `/usr/bin/python3` 缺 ROS `PYTHONPATH` 的失败已记录且不重复。
- 第一段 3.22 GB bag 只读扫描完成：无 GT Odometry/static TF，唯一 TF 为重复恒等 `map→base_link`；已提取 Radar/Livox/双 IMU frame_id，继续查下载目录说明和驱动/数据集定义。
- 官方论文/仓库证据把 `calib_radar_to_imu` 方向收敛为 Radar→VectorNav IMU；发现 body 轴还需独立定义。GT 原始 bag 未随本地下载保留，继续查官方/开源消费代码并设计仅作反证的多窗口比较。
- 完成只读来源阶段：工作区/原始下载副本 hash 一致；bag 无 GT Odometry 或 static TF，唯一 `map→base_link` TF 恒等；官方命名约定支持 Radar→VectorNav IMU，但 GT export frame 和 airborne body 轴仍未获得权威证据。
- 确认 loop3 候选数据有 6432 个稀疏 LiDAR voxel，policy 为 LiDAR 对齐、120 m 原始盒和 0.2 m 体素；下一步先沿写盘代码确认坐标轴，再建立 RED 多窗口重合诊断。
- 记录两次只读输出问题：`sort|head` broken pipe 和误打印完整 manifest；均未修改数据，后续改为限量字段读取。
- 沿真实保存调用链确认 `lidar_voxel` 稀疏 `coords` 为 XYZ 体素索引；物理点取体素中心。开始复用现有 alignment diagnostics 的输出/原子写入风格，避免重复协议实现。
- 检查现有 alignment 脚本后确认无可直接扩展的跨帧 pose 重合入口；决定新增单一职责诊断，输入整个 candidate receipt 目录而不是松散 CSV，先验证协议/hash 再计算。
- 只读统计 loop3 轨迹转角/位移后冻结默认 pair 窗口为 1.0±0.15 s、转角至少 3°、位移至多 12 m；准备写合成 RED，验证正确 GT frame 假设应具有更低的对称最近邻残差。
- 新增 3 项合成 RED：正确 GT-frame 经验残差、体素 manifest hash 篡改、fresh 输出。首次运行按预期在收集阶段因 `evaluate_mapping_pose_overlap.py` 不存在报 `FileNotFoundError`，未进入断言或创建结果；下一步实现最小 GREEN。
- 新增独立 `evaluate_mapping_pose_overlap.py`：验证 candidate audit/CSV/外参/当前 sync 与所选 LiDAR 文件 hash，统一组合 `T_local_body@T_body_lidar`，输出双向 NN 指标、经验排序和不可辨识性声明。
- overlap 合成 GREEN 3/3 通过；正确 GT-as-LiDAR fixture 残差更低，体素篡改和非空输出均在发布前拒绝。尚未运行真实 loop3 诊断。
- loop3 fresh CPU 诊断成功：6162 共同帧、1211 eligible pairs 中跨序列均匀选择 48 对，输出 60 KiB；经验排序为 GT-as-LiDAR，48/48 paired residual 均优于 GT-as-IMU，结果保持 `formal=false`。
- 诊断后代码审查发现时间基准接口不匹配：candidate pose 用 Radar time，而 LiDAR-aligned voxel 用 LiDAR time；另缺 manifest self-hash 验证。保留首轮 v1 不覆盖，下一步先补 RED/GREEN 和 Radar--LiDAR sync provenance，再生成 fresh v2。
- 已定位与本 candidate 一致的 6432 行 Radar--LiDAR sync；逐帧 Radar timestamp 与 processed sync 精确匹配。决定让 candidate v2 使用 LiDAR timestamp 并携带 sync snapshot，而不是在 overlap 阶段隐式猜测 Raw 根。
- LiDAR-time/snapshot 与 manifest self-hash RED 分别命中旧接口 `TypeError` 和“未抛异常”；GREEN 后候选 5/5、overlap 4/4 通过。v1 行为保留，v2 仅在显式 `pose_reference_sensor=lidar` 且提供严格 sync 时启用。
- 已在两个 fresh v2 目录重生真实候选与 overlap：candidate coverage 6165/6432，overlap 1244 eligible 取 48 对；GT-as-LiDAR 仍 48/48 胜出。旧 Radar-time v1 完整保留未覆盖。
- 临时敏感性复核完成并自动清理：0.5 s 为 32/32，2.0 s 为 30/32 支持 GT-as-LiDAR；两个窗口的汇总中位结论一致，长窗口存在预期 outlier，未新增永久结果目录。
- 最终新协议回归 candidate 6/6、overlap 5/5 通过；概率地图文件在首项断言前被沙箱 OpenMPI socket 限制中止，转按既有批准在沙箱外复核。
- 沙箱外概率地图 45/45 通过；两个诊断脚本及测试文件 `py_compile`、两份 CLI `--help`、`git diff --check` 均 exit 0。README、结果索引和根 TODO 三文件已同步，本阶段 scoped 工作完成。
- 用户确认在缺权威 frame 材料时采用分层方案：实现可复现的经验 LiDAR pose 离线地图合同，但 airborne/avoidance formal 继续 fail-closed。本轮开始审计现有 streaming map 入口，不修改训练或权重。
- 已读完 streaming map 参数预检、pose loader、逐帧更新和 `map_run.json` 调用链；确认不能用“LiDAR pose + 单位 LiDAR→body”伪装现有 body 合同，将新增与 `--formal_mapping` 互斥的 offline empirical 模式。
- 已核对真实候选/重合 audit：6165 帧具备无外推的 LiDAR 时间位姿，48 对重合诊断经验支持 GT-as-LiDAR；两份来源都明确非 formal，后续 receipt 将保留该证据等级并显式设置 `airborne_formal=false`。
- 已确认测试目录规则；本轮仅新增单元/小型集成测试与 `test/result/comparison/alignment_check/` 下的新鲜合同结果，不执行训练、全量推理或覆盖历史结果。
- 核心接口审查收敛：`update_from_voxel()` 当前无 direct pose 参数，snapshot 固定保存 body 链矩阵；streaming 的 strict 预检、mask 预检、逐帧更新、查询原点和 metadata 都只识别 `formal_mapping`。下一步用 RED 测试固定 direct `T_local_voxel` 的互斥语义和经验 receipt 的 fail-closed 行为。
- 发现并纳入 6165/6432 帧接口差异：offline empirical 模式将由 receipt 精确选择 inference 的有序帧子集，不允许用 `frame_limit`，也不要求删除 267 个无 pose 的推理文件。
- 新增 7 项经验合同/direct-pose RED 测试；其中 3 项合同用例稳定因 `diffusion_consistency_radar.empirical_pose_contract` 不存在而 ERROR，确认目标接口尚未实现。首轮继续执行地图核心时遇到沙箱 OpenMPI socket 限制，已隔离为环境问题，GREEN 后按既有批准方式复核。
- 已实现经验合同模块与发布 CLI 初版：构建前验证 candidate/overlap/sync/pose/extrinsic 哈希和诊断语义，输出自包含来源快照与 direct `lidar_to_local.csv`，运行时重新组合核验并按 receipt 选择有序帧子集。
- 概率地图核心已增加与 body 链互斥的 `T_local_voxel` 参数，并在 snapshot 显式记录 `direct_local_voxel` 与 `last_body_pose_available=false`；原有 body 链默认调用保持不变。正在接入 streaming 严格模式。
- 经验合同 3/3 GREEN，1--2 帧 streaming 严格集成 2/2 GREEN：完整 inference 有 2 帧、receipt 仅选择 1 帧时可无删除地融合 direct LiDAR pose，map receipt 明确 offline empirical 且 airborne/avoidance formal 均为 false。
- 沙箱外完整新接口 6/6、原概率地图兼容回归 45/45 通过；原 formal pose+外参组合、mask 篡改拒绝和 legacy 功能均未回退。
- 已从真实 loop3 LiDAR-time candidate v2 与 overlap v2 发布 fresh `empirical_lidar_pose_loop3_v1`：receipt SHA-256=`13977fe4...cb432`，direct pose SHA-256=`46719ffc...17d46`，验证 6432 available 中严格选择 6165 帧、267 帧保持 uncovered，`airborne_formal=false`。
- 第一轮代码审查发现 prediction provenance 隐形依赖：正式 inference receipt 只绑定 mask，未绑定地图输入 voxel 内容。下一步先补 prediction receipt RED/GREEN，再重跑 formal inference/map 兼容测试；不需要模型前向或重训。
- prediction provenance RED 已稳定复现：metadata 新参数在旧实现报 `TypeError`、缺 receipt 未拒绝，且地图在 prediction 文件被替换后仍完成输出。三种失败分别证明生产、必需性和消费端校验都尚缺失。
- prediction receipt 聚焦 GREEN：2 项 inference metadata 与 2 项 formal map 测试通过；地图现能在输出创建前拒绝 prediction voxel 内容篡改，并继续通过原 pose+外参+mask 完整 fixture。
- prediction 协议已抽为共享轻量模块，消除 inference/map 摘要算法重复；完整多模态推理接口 37/37、概率地图 46/46、经验合同 6/6 通过，真实 loop3 receipt 在强化来源交叉校验后仍通过 6432/6165/267 自检。
- 收尾复核完成：builder/map 两个 CLI `--help` 成功；真实 loader 返回 first=`000005_voxel.npy`、last=`006431_voxel.npy`、`airborne_formal=false`；根 README、test README、结果索引与 TODO 三文件均已更新。
- 最终 `git diff --check` 通过，共享 prediction 协议仅保留一份实现；未启动训练、模型 forward、GPU 推理、全量预处理或正式地图回放。
- 用户确认继续 point-count/Doppler-validity sidecar，并要求交付下一步训练脚本；已冻结为“先数据合同、后模型迁移”，本轮不自动训练或全量重建。
- 已定位预处理真实写盘链、manifest 精确模态约束和 Dataset loader；初步选择在现有 Radar NPZ 内增加稀疏统计键，避免破坏 deployment v3 的精确目录合同，下一步核验 loader/manifest/checkpoint 身份细节。
- 调用链审计确认可复用 voxelizer 的 `unique_counts`，统计内容将由既有 radar 文件 hash→manifest hash→formal data identity 自动绑定；决定不升级 checkpoint schema，只新增正式 Dataset 的显式统计门禁和批内摘要。
- 已核对 Dataset 所有生产调用与 8 GB launcher：unified/CD formal 都需接入统计门禁；旧 8 GB 脚本是 full120 legacy candidate，不能直接交付为 0--80 m sidecar 训练入口。
- Radar statistics RED 5/5 稳定失败：3 项缺 `radar_statistics` 模块，2 项 voxelizer 缺 `return_statistics` 参数；尚未修改生产代码，失败精确覆盖目标接口。
- 初版 GREEN 完成：新增共享统计协议、voxelizer 可选统计返回、Radar NPZ 同文件持久化、Dataset 全帧门禁/摘要与 collate 透传；静态编译及新增 5/5 测试通过，四通道逐值保持不变。
- formal unified/CD/default config/train launcher 已强制 statistics 门禁；聚焦回归通过：statistics 5、CD entrypoint、VAE checkpoint 24、mini launcher protocol 11，相关 Python 静态编译通过。
- 真实 garden 4 帧 CPU smoke 已发布到 `test/result/comparison/radar_statistics_smoke_v1`：training manifest v2 hash=`a945c2fd...d4a8`，Dataset 4 帧严格加载通过，模型 Radar tensor 仍为 `[4,32,128,128]`。
- 四帧 point totals=`770/770/762/761`，Doppler-valid totals 完全相同，multi-point voxel=`129/130/142/138`；NPZ 只新增 protocol/point_count/doppler_valid_count 三键，模型明确未消费统计。
## 2026-08-27 Radar 统计契约：训练入口审查

- 已确认正式训练脚本存在固定 `CUDA_VISIBLE_DEVICES=0,1` 的单机接口不匹配。
- 已确定最小修复范围：训练 launcher 的可配置 GPU 与只预检模式、Radar 统计摘要引用边界、对应单元/静态协议测试。
- 下一步：实施上述小步修改，执行 CPU/静态回归，不运行训练。
- 已定位三处硬编码 GPU：VAE/LDM 使用 `0,1`，CD 使用 `0`；将统一为经格式检查的 `CUDA_DEVICES`。
- 已确认 launcher 的预检插入点位于 normalization artifact 校验之后、运行配置生成之前。
- 已检查共享 Radar statistics 加载器、Dataset 元数据生成点和 launcher 静态协议测试；下一步先补 RED 断言，再修改实现。
- RED（launcher）：`test_mini_scripts_protocol.py` 11 项中 1 项按预期失败，证明当前脚本缺少可配置单卡、`PREFLIGHT_ONLY` 与 Radar statistics 全量预检；测试链因 `&&` 在首个失败处停止。
- RED（Dataset metadata）：`test_radar_statistics_protocol.py` 5 项中 1 项按预期报 `KeyError: reference`，证明增强前/模型未消费的边界尚未进入返回合同。
- 已实施：launcher 支持经校验的 `CUDA_DEVICES`（默认单卡 0）、`PREFLIGHT_ONLY=1`、逐 manifest Radar NPZ 统计预检和 formal data protocol 重建核对；预检在运行配置写入前退出。
- 已实施：Radar 统计摘要类型改为 `Dict[str, object]`，Dataset 元数据标记 `reference=pre_augmentation_persisted_radar_voxel` 与 `model_consumed=false`。
- GREEN 第一段：`bash -n` 和 launcher 协议测试 11/11 通过。随后第二个 `conda run` 在导入阶段受本机 Open MPI 无网络接口错误中断，尚不能计为代码测试结果，将用项目环境 Python 的隔离模式单独重跑。
- 在沙箱外以同一 Conda 环境 Python 重跑 Radar statistics 测试，5/5 通过；确认此前失败仅来自沙箱禁止 socket 导致的 Open MPI 初始化问题。
- 代码审查确认正式 v2 数据目录和 normalization artifact 当前均未生成；不会运行 launcher 预检或训练。下一步将补文档和 TODO，给出“预处理 → hash → 只预检 → 服务器 VAE”顺序。
- 已复核 `preprocess-v2.sh` 的 8 步实际参数与 fresh-output 门禁，准备按真实脚本而非旧 README 更新操作说明。
- 已更新根 README 与 TODO 三份记录，明确旧 full120 mini 为 legacy，并记录 formal v2 的预处理、SHA、只预检和服务器 VAE 顺序。
- 已重新读取 `test/AGENTS.md`；最终验证限定为编译、shell 语法和聚焦单元/接口测试，不运行全量预处理、模型 forward、训练或完整推理。
- 最终静态检查通过：相关 Python `py_compile`、`train_unified.sh`/`preprocess-v2.sh` 的 `bash -n`、`git diff --check`。
- 最终回归通过：launcher 11、Radar statistics 5、Dataset metadata 15、manifest 12、temporal/formal data 4、VAE checkpoint 24，共 71 项；CD 入口与多模态 CD 接口两份直接脚本通过。
- 最终代码审查完成：确认统计 policy、formal config、unified/CD 构造参数和精确 garden 场景调用链一致；最后一次 `git diff --check` 通过。
